doctype
html(lang="es")
    head
        meta(http-equiv="Content-Type" content="text/html" charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Pedido
        style(type='text/css').
            table {border-collapse:separate;}
            a, a:link, a:visited {text-decoration: none; color: #00788a;}
            a:hover {text-decoration: underline;}
            h2,h2 a,h2 a:visited,h3,h3 a,h3 a:visited,h4,h5,h6,.t_cht {color:#000 !important;}
            .ExternalClass p, .ExternalClass span, .ExternalClass font, .ExternalClass td {line-height: 100%;}
            .ExternalClass {width: 100%;}
    body
        table(cellpadding="0" cellspacing="0" width="80%" style="background-color: #f2f2f2;border-collapse: collapse; margin-left: 10%;")
            tr(bgcolor=`${color}`)
                td(height="50px")
            tr
                td(style="padding: 20px 0px 0px 20px;text-align:center;")
                    h3 Mensaje de contacto en #{empresa.nombre}
                    p Correo electrónico: #{contact.email}
                    p Nombre: #{contact.nombre}
                    p Asunto: #{contact.asunto}
                    p Mensaje:#{contact.mensaje}

            tr(bgcolor=`${color}`)
                td(style="padding: 20px 0px 0px 20px;")
                    table(cellpadding="0" cellspacing="0" width="100%" style="border-collapse: collapse;")
                        tr
                            td(style="text-align:center; color: #ffffff;" height="50px")
